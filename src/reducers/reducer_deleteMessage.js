import {DELETE_MESSAGE} from '../constants.js';

export default function (state=[], action){
     switch(action.type){
         case DELETE_MESSAGE:
            return state ;

            default: 
              return state 

     }
}